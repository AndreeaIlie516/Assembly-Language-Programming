DATA SEGMENT PARA PUBLIC 'DATA'
DATA ENDS

STCK SEGMENT PARA STACK 'STACK'
      STACK_START LABEL WORD
STCK ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
   ENTRY PROC NEAR
	ASSUME CS:CODE, DS:DATA
	PUSH DS
	XOR AX,AX
	PUSH AX
	MOV AX,DATA
	MOV DS,AX
	
	MOV AX,20
	XOR DX,DX 	; clear DX
	ADD DX,AX	
	SHL DX,1	; *2
	SHL DX,1	; *4
	SHL DX,1	; *8
	SUB DX, AX	; *7
	MOV AX,DX	; 7*AX	
	XOR BX,BX	; clear BX	
	MOV BX,40	
	SHL BX,1	; *2
	SUB AX,BX	; 7*AX-2*BX
	XOR BX,BX	; clear BX
	MOV BX,40	
	SHR BX,1	; /2
	SHR BX,1	; /4
	SHR BX,1	; /8
	SUB AX,BX	; 7*AX-2*BX-BX/8
	
   ENTRY ENDP
	RET
START ENDP
CODE ENDS
END START