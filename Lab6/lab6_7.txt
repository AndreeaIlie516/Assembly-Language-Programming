DATA SEGMENT PARA PUBLIC 'DATA'
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
ASSUME CS:CODE, DS:DATA
PUSH DS
XOR AX,AX
PUSH AX
MOV AX,DATA
MOV DS, AX

	PUSH 1			
	PUSH 2
	PUSH 3
	PUSH 4
	PUSH 5
	PUSH 6
	PUSH 7
	PUSH 8
	PUSH 9
	PUSH 10

	MOV CX, 10
	MOV BP, SP
	MOV SI, 18
	L1:
	MOV AX, [BP+SI]				
	PUSH AX			
	SUB SI, 2		
	LOOP L1

	MOV CX, 20
	L2:
	POP AX			
	LOOP L2

RET
START ENDP
CODE ENDS
END START