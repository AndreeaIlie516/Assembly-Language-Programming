DATA1 SEGMENT
	SIR1 DB 1,2,5,7,9
DATA1 ENDS
DATA2 SEGMENT
	SIR2 DB 5 DUP(0)
DATA2 ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA1, ES:DATA2
START PROC FAR
	PUSH DS
	XOR AX,AX
	PUSH AX
	MOV AX,data1
	MOV DS,AX
	MOV AX,data2
	MOV ES,AX

	MOV CX,5		
	MOV SI, 0
	L1:   MOV AL,SIR1[SI]
	      MOV SIR2[SI],AL	
	      INC SI		
	      LOOP L1

	RET
START ENDP
CODE ENDS
END START