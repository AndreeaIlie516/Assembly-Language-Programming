DATA SEGMENT PARA PUBLIC 'DATA'
MEM DB 5 DUP(?)
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
ASSUME CS:CODE, DS:DATA
PUSH DS 
XOR AX, AX
PUSH AX
MOV AX, DATA
MOV DS, AX

	MOV CX, 5
	MOV SI, 0

	L1:
	MOV AL, CL		
	MOV MEM[SI], AL		
	INC SI	
	LOOP L1
	
RET
START ENDP
CODE ENDS
END START