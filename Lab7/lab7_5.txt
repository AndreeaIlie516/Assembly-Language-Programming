DATA SEGMENT PARA PUBLIC 'DATA'
   OUTP DB ?
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
START PROC FAR
ASSUME CS:CODE, DS:DATA
PUSH DS 
XOR AX,AX
PUSH AX
MOV AX,DATA
MOV DS, AX

	MOV AX, 427		
	MOV CX, 10
	MOV SI,0

	L1:
	CWD			
	DIV CX 			
	MOV OUTP[SI], DL
	ADD OUTP[SI], '0'
	INC SI
	CMP AX, 0
	JNE L1

	MOV CX, SI
	MOV AH, 02H 		
	DEC SI

	L2: MOV DL, OUTP[SI]
	INT 21H
	DEC SI
	LOOP L2

RET
START ENDP
CODE ENDS
END START